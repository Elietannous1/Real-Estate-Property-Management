<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Messages - Real Estate Management</title>
  <link 
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" 
    rel="stylesheet">
  <style>
    body {
      background: linear-gradient(to right, #4e73df, #1cc88a);
      color: #fff;
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
    }

    .container {
      margin-top: 50px;
      background: rgba(255, 255, 255, 0.9);
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
    }

    .message-list {
      max-height: 400px;
      overflow-y: auto;
      padding: 10px;
      background: #f8f9fc;
      border-radius: 10px;
    }

    .message {
      display: flex;
      align-items: flex-start;
      margin-bottom: 15px;
    }

    .message-user {
      margin-right: 15px;
      background: #4e73df;
      color: #fff;
      padding: 10px;
      border-radius: 50%;
      width: 40px;
      height: 40px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: bold;
    }

    .message-content {
      background: #e2e8f0;
      color: #333;
      padding: 15px;
      border-radius: 10px;
      max-width: 70%;
    }

    .message-date {
      font-size: 0.9rem;
      color: #888;
      margin-top: 5px;
    }

    .send-message-form {
      display: flex;
      align-items: center;
      margin-top: 20px;
    }

    .send-message-input {
      flex-grow: 1;
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 10px;
      margin-right: 10px;
    }

    .send-message-button {
      background: #1cc88a;
      color: #fff;
      border: none;
      padding: 10px 20px;
      border-radius: 10px;
      cursor: pointer;
      transition: background 0.3s ease-in-out;
    }

    .send-message-button:hover {
      background: #17a673;
    }
  </style>
</head>
<body>
  <div class="container">
    <%- include('partials/navigationBar') %>
    <h2 class="text-center mb-4">Messages</h2>
    <div class="message-list">
      <% if (message.length > 0) { %>
        <% message.forEach((message) => { %>
          <div class="message">
            <div class="message-user">
              <%= message.id %>
            </div>
            <div>
              <div class="message-content">
                <%= message.content %>
              </div>
              <div class="message-date">
                Sent on: <%= new Date(message.dateSent).toLocaleDateString() %>
              </div>
            </div>
          </div>
        <% }); %>
      <% } else { %>
        <p class="text-center">No messages found.</p>
      <% } %>
    </div>
    <form class="send-message-form" action="/send-message" method="POST">
      <input
        type="text"
        class="send-message-input"
        name="messageContent"
        placeholder="Type your message..."
        required
      />
      <button type="submit" class="send-message-button">Send</button>
    </form>
  </div>
</body>
</html>
